# Testing API calls in Python

A repository that I will populate with information for the purposes of testing GitHub API calls

The talk itself can be found in the Jupyter notebook - with the RISE plug-in it will become the slides by typing `alt+r` (no idea what that means for Mac users, sorry)

`api_calls.py` is the module we need to create for `pytest-recording` to properly interact and create cassettes
